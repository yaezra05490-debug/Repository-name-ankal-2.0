# מדריך שימוש — אנק״ל

## התחלה

בדף הפתיחה שני כפתורים:

- `פתחו את אנק״ל` — עבודה באתר, בלי להתקין כלום.
- `הורדת התוכנה למחשב` — גרסת Windows שעובדת גם בלי אינטרנט.

בכניסה הראשונה נשאלת שאלה אחת: **`כניסה עם Google`** או **`עבוד במצב אופליין`**.

- **עם חשבון Google** — הרשימות נשמרות בענן, זמינות מכל מחשב, ולא הולכות לאיבוד אם משהו קורה למחשב.
- **במצב אופליין** — הכול נשאר במחשב הזה בלבד, בלי חשבון ובלי אינטרנט.

השאלה נשאלת פעם אחת בלבד. אפשר לשנות בכל רגע מכרטיס החשבון בתחתית התפריט, וגם מי שהתחיל אופליין יכול להתחבר מאוחר יותר ולהעלות את הרשימות.

במסך הרשימות צרו רשימה או ייבאו קובץ.

## איפה מתחילים

מיד אחרי `אנשי קשר` בתפריט נמצא **`ניהול חכם`** — זו הדרך המומלצת. הוא סורק את הרשימה לבד ואומר מה צריך תיקון, במקום שתחפשו לבד. הדפים `ניקוי והחלפה` ו-`בדיקת כפולים` הם לעבודה ממוקדת כשכבר יודעים מה רוצים, ובשניהם יש כפתור שמעביר לניהול החכם.

## ייבוא

1. בחרו VCF, XLSX, XLS או CSV.
2. ב־Excel בחרו גיליונות, שורת כותרות ומיפוי לשבעת השדות.
3. אותה עמודה אינה יכולה להיבחר פעמיים.
4. אם מקום מלא, תראו ערך קיים וחדש ותבחרו להחליף או לחזור.
5. חלון החלטה חשוב אינו נסגר בלחיצה בחוץ או Escape. הכפתור `ביטול הייבוא וסגירת החלון` מבטל הכול.
6. ברשימה קיימת בחרו הוספה, החלפה או ביטול.

כל תא הוא ערך אחד ואינו מפוצל לפי פסיקים. איש קשר ללא שם נכנס בשם `ללא שם` ומופיע בדוח.

## עריכה

אנשי הקשר מוצגים בכרטיסים במחשב ובטלפון. לחצו כרטיס לעריכה בשבעת השדות: שם, נייד, בית, עבודה, פקס, מייל והערה. השינויים נשמרים אוטומטית. `בטל` ו־`בצע שוב` מטפלים בפעולה האחרונה.

## בדיקת איכות: סימונים וכפולים

שני הדפים עובדים באותה שיטה. `ניהול חכם` עושה בדיקה מלאה — קודם הסימונים בשמות ואחר כך הכפולים; `בדיקת כפולים` מדלג ישר לכפולים.

**איך זה עובד**

1. לוחצים `בדיקה מלאה`. בסוף הסריקה מופיע מסך סיכום עם כרטיס לכל סוג בעיה וכמה נמצאו מכל סוג. אפשר ללחוץ על כרטיס כדי לקפוץ ישר לסוג הזה.
2. לוחצים `בוא נתחיל` ועוברים סוג-סוג, מהוודאי אל המסופק.
3. בכל סוג יש שתי דרכים:
   - **לטפל בכולם בבת אחת** — למשל `הסר מכולם` לסימון, או `מזג את כולם` לכפולים זהים. ההצעה שמיושמת היא בדיוק זו שמוצגת במסך הפריט.
   - **לעבור אחד אחד** — מסך לכל פריט, עם אפשרות לדלג. המסך מציג גם את המספרים והמייל של הכרטיס (מספר שכתוב בשני שדות מוצג פעם אחת), ויש בו כפתור `מחק את איש הקשר`.
4. במסך של קבוצת כפולים יש פח 🗑 בכל שורה — אפשר למחוק אחד מהכרטיסים במקום למזג — וליד כל ערך בטבלה: ✎ לעריכת הפרט (שם, מספר, מייל או הערה) ו־✕ למחיקתו מהכרטיס שלו בלבד. אחרי כל שינוי המסך מציג מיד את המצב החדש, וקבוצה שנשאר בה כרטיס אחד נפתרת מעצמה. הכל ניתן לביטול עם `בטל`.
5. `דלג על הסוג הזה` מדלג על כל הקטגוריה.
6. בכל חלון במערכת, `Enter` מאשר (מפעיל את הכפתור הראשי) ו־`Escape` סוגר.

**סוגי הכפולים, לפי הסדר**

| הסוג | מה זה | אישור גורף |
|---|---|---|
| זהים לגמרי | אותו שם ואותם פרטים | כן |
| מיזוג בטוח | אין סתירה, רק מידע שחסר בכרטיס אחד | כן |
| שם שונה | אותו מספר, השם נכתב אחרת | כן — לפי השם הנקי |
| מספרים שונים | אותו שם, מספרים שונים | כן — לפי שיוך המספרים המוצע |
| שם ומספרים שונים | שתי ההחלטות יחד | כן |
| מייל או הערה שונים | שני ערכים שונים באמת | לא — חייבים לבחור |
| שם דומה, מספרים שונים | אולי אותו אדם ואולי לא | לא — חייבים להחליט |

**מה המערכת מציעה לבד**

- **שם**: השם הנקי ביותר. `מרים הריס` ינצח את `מרים הריס_1`, גם אם הוא קצר יותר.
- **מספרים**: כל מספר משויך למשבצת שהקידומת שלו מכתיבה — `05x` לנייד, `02/03/04/08/09` ו־`07x` לקווי. שני מספרים לא יקבלו את אותה משבצת, ומספר שאין לו קידומת מוכרת נשאר במקום שבו הוא כבר יושב. אפשר לשנות כל שיוך בתפריט שלידו.
- **כתיבת המספר**: אם אותו מספר מופיע בשתי צורות, נשמרת הצורה התקינה — `0501234567` ולא `501234567` שאקסל אכל לו את האפס.

`אלה אנשים שונים — השאר נפרדים` נשמר ברשימה ולא יחזור בסריקה הבאה, גם בקבוצה של שלושה ומעלה.

## ניקוי

אפשר להוסיף טקסט לשמות או לבצע חיפוש והחלפה. תצוגה מקדימה מופיעה לפני הביצוע. Regex כבד מדי נעצר בתהליך נפרד בלי שינוי חלקי.

## מצבי שמירה

- `נשמר במחשב` — העותק המקומי מעודכן.
- `ממתין לסנכרון` — השינוי שמור מקומית ומחכה.
- `מסנכרן` — מתבצעת שליחה.
- `נשמר בענן` — השרת אישר.

בהתנגשות בין מכשירים אפשר לשמור עותק חדש או לפתוח את גרסת הענן.

## ייצוא

VCF מתאים לטלפון; Excel יוצר שבע עמודות; CSV כולל עברית והגנה מפני הפעלת תוכן כנוסחה.

## Windows

התוכנה נפתחת ישר בסביבת העבודה. כניסה עם Google נפתחת בדפדפן הרגיל. עדכון מוצע ואינו נכפה. תוכנה לא חתומה עשויה להציג אזהרת Windows.
