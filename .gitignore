# Node נייד — מגיע עם התיקייה, לא נכנס ל-git (95MB)
/node/
node_modules/
dist/
*.log
.DS_Store

# מכיל את סוד הלקוח של גוגל. המאגר ציבורי, ולכן הקובץ נשאר מקומי.
# התבנית למילוי היא app-config.example.json
/app-config.json
